---
import Layout from '../layouts/Layout.astro';
import SignalsTest from '../components/SignalsTest.jsx';
import Header from '../components/Header.tsx';
import Footer from '../components/Footer';
import Hr from '../components/Hr';
---

<Layout title="Vaidya Signal">
	<Header active={undefined}/>
	<main class="mx-auto max-w-3xl flex flex-col px-4 py-8 gap-6 text-skin-base">
			<p>The <span class="font-bold underline underline-offset-2 decoration-2">Vaidya Signal</span> is a bottom reversal signal which is triggered when the following conditions are met:</p>
			<div class="pl-8">
				<p class=""><strong>1.</strong> the current low * (L2) is lower than the previous low (L1)</p>
				<p><strong>2.</strong> MACD and RSI are both higher at L2 than they were at L1</p>
				<p><strong>3.</strong> volume at L2 is lower than it was at L1</p>
				<p class="pt-2 pl-8 text-xs italic">* a low is defined as having 3 days before and after whith higher closes</p>
			</div>
			
			<div class="pt-8 grid grid-cols-1 sm:grid-cols-2 gap-4">
				<div class="flex flex-col gap-4 sm:border-r-[1px] border-skin-line">
					<h2 class="text-2xl font-bold text-center">RECENT</h2>
				</div>
				<div class="flex flex-col gap-4">
					<h2 class="text-2xl font-bold text-center">SEARCH</h2>
				</div>

			<!-- <SignalsTest client:load /> -->
	</main>
	<Footer />
</Layout>